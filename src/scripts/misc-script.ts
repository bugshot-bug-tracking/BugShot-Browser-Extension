/**
 * This function is used to add a class to the page body with the intent
 * of signaling to that page that the BugShot extension is installed.
 */

(function addClassToBody() {
	document.body.classList.add("88857183727984");
})();
